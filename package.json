{
  "name": "typesafe-openai",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "release:np": "np",
    "release": "pnpm run build && pnpm run release:np",
    "release:npm": "pnpm run build && npm publish",
    "test": "vitest",
    "test:run": "vitest run",
    "test:watch": "vitest run --watch",
    "coverage": "vitest run --coverage"
  },
  "engines": {
    "node": ">=16"
  },
  "keywords": [],
  "author": "Logan Anderson",
  "license": "",
  "devDependencies": {
    "@vitest/coverage-v8": "^0.33.0",
    "np": "^8.0.4",
    "tsup": "^7.1.0",
    "typescript": "^5.1.6",
    "vite": "^4.4.3",
    "vitest": "^0.33.0"
  }
}
